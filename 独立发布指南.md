# DIAL-BAM Method 独立发布指南

> 将 `dial-bam-method` 拆分为独立 GitHub 项目的完整步骤。

---

## 一、项目结构（独立发布后）

```
dial-bam-method/
├── README.md
├── README_EN.md
├── LICENSE
├── CONTRIBUTING.md
├── package.json
├── index.html              # 入口，跳转 tools/
├── 独立发布指南.md          # 本文件
├── docs/
│   ├── BAM.md
│   ├── DIAL.md
│   ├── 内核与延伸.md
│   ├── 与BMAD的关系.md
│   └── 参考框架与AI时代.md
├── tools/
│   ├── index.html          # 工具导航中心
│   ├── 洞察可视化.html      # PESTEL/五力/SWOT
│   ├── DIAL-流程可视化.html # 流程图
│   ├── BAM-追溯矩阵.html
│   ├── BAM-追溯矩阵-模板.json
│   ├── BAM-导入指南.md
│   ├── BAM-工作流检查清单.md
│   └── DIAL-规划与任务模板.md
└── scripts/
    └── init.js
```

---

## 二、发布步骤

### 步骤 1：创建新仓库

1. 在 GitHub 创建新仓库，如 `dial-bam-method`
2. 选择 Public，不初始化 README

### 步骤 2：复制或移动项目

**方式 A：从当前工程复制**

```bash
# 在 IP 项目根目录
cp -r dial-bam-method /path/to/new-repo/dial-bam-method
cd /path/to/new-repo/dial-bam-method
```

**方式 B：直接作为新仓库根目录**

```bash
# 将 dial-bam-method 内容作为新仓库根
mkdir dial-bam-method-standalone
cp -r dial-bam-method/* dial-bam-method-standalone/
cd dial-bam-method-standalone
```

### 步骤 3：初始化 Git 并推送

```bash
git init
git add .
git commit -m "feat: initial DIAL-BAM Method"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/dial-bam-method.git
git push -u origin main
```

### 步骤 4：更新配置

| 文件 | 替换内容 |
|------|----------|
| `package.json` | `repository.url` 中的 `YOUR_USERNAME` |
| `CONTRIBUTING.md` | Issues 链接中的 `YOUR_USERNAME` |
| `LICENSE` | 第 3 行补充版权持有人 |

### 步骤 5：可选 - 发布到 npm

```bash
npm login
npm publish --access public
```

发布后可执行：`npx dial-bam-method init`

---

## 三、本地预览

```bash
cd dial-bam-method
npm run serve
```

访问 http://localhost:3789

- 根路径自动跳转至 `tools/index.html` 工具中心
- 可访问：洞察可视化、DIAL 流程、BAM 追溯矩阵

---

## 四、与原工程的关系

| 场景 | 说明 |
|------|------|
| **保留在 IP 工程内** | dial-bam-method 作为子目录，可 git subtree 或 submodule 管理 |
| **独立发布** | 复制到新仓库，作为独立项目维护 |
| **双向同步** | 独立发布后，原工程可引用为 submodule 或通过 npm 引入 |

---

## 五、GitHub 仓库设置建议

- **Description**：DIAL + BAM 产品与创业方法论，洞察→战略→业务需求→数字化，参考 BMAD
- **Topics**：`methodology`、`bam`、`dial`、`bmad`、`lean-startup`、`business-architecture`、`product-development`
- **Website**：若启用 GitHub Pages，可填 `https://YOUR_USERNAME.github.io/dial-bam-method/`
